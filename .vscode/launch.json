{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Agent Server",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "researcher.main:app",
                "--host", "0.0.0.0",
                "--port", "8000",
                "--reload"
            ],
            "cwd": "${workspaceFolder}/src",
            "envFile": "${workspaceFolder}/.env",
            "jinja": true
        }
    ]
}
